server.servlet.context-path=/api

# Caching is configured programmatically in CacheConfig (Caffeine, named
# caches with per-cache TTLs). The previous spring.cache.* properties defined a
# single unbounded "cache" and a misspelled TTL key that was silently ignored.
